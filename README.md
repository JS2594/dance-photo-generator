# PopShot V17 Mobile Demo

本版按确认后的手机 Demo 重构，目标是让小白用户直接使用。

## 默认流程
上传课后合照 → 选择课程 → 基础美化 → 一键生成 → 默认成品组合。

## 关键规则
- 默认优先显示「成品组合」。
- 成品组合出现时，不额外叠加自定义 Q 版人物和课程文字。
- 「组合」中可切换：成品组合（推荐） / 自定义组合。
- 成品组合只调整整体大小；自定义组合可选择 Q 版人物、课程字样并拖动/缩放。
- 首页保留四个课程：Lelepop / ButtScaler / ZUMBA / ZUMBA CAMP。
- 基础美化固定单行横向滑动，不换行。
- 主操作区为：组合 / 相框 / 贴纸 / 调整照片 / 更多。
- 所有主要按钮扩大手机点击区域。
- 输出画布仍为 2525 × 1894。

## 后续人工替换成品组合
直接替换以下目录中的 PNG，文件名保持不变即可：
- public/custom-combos/lelepop/
- public/custom-combos/buttscaler/
- public/custom-combos/zumba/
- public/custom-combos/zumba-camp/

不需要自己拼接代码。

## V17.3 清晰度专项
- 本轮只优化清晰度链路，不调整色彩参数。
- 最终成品不再固定 2525×1894。
- 保持 4:3，按当前裁剪区域的原始有效像素决定输出尺寸。
- 手机 Canvas 仅用于预览；保存时创建独立高分辨率 Canvas，从原始照片与原始素材重新绘制。
- PNG 无损导出，不设置 JPEG quality，不做有损压缩。
- 不把低分辨率裁剪区域强制放大成假高清。
- 页面会显示“原图尺寸 → 实际导出尺寸”，方便直接核验。

## V17.4 清晰度锁定 + 课程响应
- 锁定原图级 4:3 PNG 输出；默认使用原图最大可用 4:3 裁剪区域。
- 人脸识别只影响裁剪位置，不再把裁剪区域缩成几百像素。
- 默认 photoZoom=1；只有用户主动放大才减少有效输出像素。
- 本轮没有调整任何色彩参数。
- 课程卡片移除下方说明小字。
- 课程点击后立即更新选中态，不等待成品组合 PNG 加载。
- 快速连续切课程加入请求序号保护，旧素材请求不会覆盖最后选择。
- 空闲时预热四个课程的第一张默认搭配，提高第一次切换速度。
- 一键生成/换一版增加 busy 状态，避免连续点击造成重复任务。

## V17.5
- 默认构图适度放大真人，并尽量保留左右各约一个站位。
- 默认搭配固定使用每个课程 custom-combos 文件夹中的 *_01.png；“换一版”才轮换其它人工成品组合。
- 一键换风格仅在自定义搭配生效。
- 贴纸默认更大，添加后可直接在成图上拖动；推荐与通用分离。
- 新增 public/sticker-library/，可按 README 直接补充 sticker_01.png ~ sticker_30.png，缺图自动忽略。

## V1.0.3 修复
- 只有线上版本高于当前版本时才提示更新；当前版本更高时显示部署同步中。
- “更新并刷新”会注销旧 Service Worker、清空 Cache Storage 后重新加载。
- 修复草稿恢复弹窗“继续编辑”按钮白字白底。
- 默认搭配锁定各课程 `_01.png` 成品组合。
- 默认搭配加载失败时不再静默切为自定义搭配，而是明确提示素材未加载。
- 四个课程默认搭配提前预加载。
- 本版不改清晰度、不改色彩。

# PopShot V1.0.3 Clean Reset

这是重新整理的“从零部署”版本，功能基于上一版稳定功能，但公开版本号从 V1.0.3 重新开始。

## 正确上传方式
请不要把 `PopShot_V1_0_UPLOAD_x_of_4` 文件夹本身上传到仓库。
每个 ZIP 解压后，只把 ZIP **里面的内容** 上传到 GitHub 仓库根目录。

最终根目录应该直接看到：
- index.html
- app.js
- styles.css
- manifest.webmanifest
- service-worker.js
- version.json
- popshot-config.json
- public/
- assets/
- icons/
等。

## 建议
如果仓库已经清空，按 Part 1 → Part 2 → Part 3 → Part 4 的顺序，每一包：
解压 → 上传里面的内容 → Commit → 回仓库首页。

## V1.0.3 稳定修复版
本版在 V1.0 完整功能基础上修复，不删功能：
- 保留原图级 4:3 PNG 无损高清导出。
- 保留默认搭配 / 自定义搭配、相框、贴纸、调整照片、课程切换、保存、草稿恢复。
- 修复照片读取卡住：首帧先显示原图，默认搭配和主体识别改为后台异步，均有超时兜底。
- 修复安装到桌面后 Lelepop 课程形象图可能不显示，增加缓存版本号与 fallback。
- 默认搭配严格读取四个课程对应 `_01.png` 固定成品组合，不再静默变成自定义搭配。
- 默认构图放大真人主体，并按识别结果尽量保留左右各约一个人站位。
- 本版不调整色彩参数。

## V1.0.3 上传修复
- 修复 V1.0.1 上传处理器引用 `currentPhotoObjectURL / photoLoadSeq` 但变量未声明的问题。
- iPhone Safari / 安装到桌面 WebApp 的 file input 不再使用 `display:none`，改为透明覆盖整个上传区域，提升点击可靠性。
- 保留 V1.0.1 的原图级 4:3 PNG 无损高清导出、固定默认搭配、人物放大构图、相框、贴纸、自定义搭配、草稿等功能。
- 不修改色彩参数。

## V1.0.3 功能回归修复
- 修复默认搭配：四门课程严格读取用户固定 `_01.png` 成品组合，不再随机拼独立 Q 版人物 + 课程文字。
- 默认搭配加载期间不再短暂显示自定义搭配。
- 修复人物构图：尽可能放大群像，左右各保留约一个人站位；减少多余场地。
- 修复高清导出：最终导出尺寸取原图最大可用 4:3 像素，不再因自动构图从 1706×1279 降成 1444×1083。
- 手机预览与最终高清导出继续分离，PNG 无损。
- 修复课程切换“正在切换…”状态不结束。
- 保留 V1.0.2 已修复的手机上传功能。
- 保留默认/自定义搭配、相框、贴纸、拖动调整、照片调整、草稿恢复、PWA 等功能。
- 一键换风格仍只作用于自定义搭配。
- 本版不修改色彩参数。


## V1.0.6
- 默认方案 A 改为“鲜活自然”：降低过度饱和，轻提对比/阴影/肤色观感。
- 美化与调色支持亮度、对比、饱和、色温、高光、阴影。
- 新增“保存为我的常用”，下次优先读取本机配置；可一键恢复默认 A。
- 保留 V1.0.3 高清导出、4:3 构图、固定默认搭配、自定义搭配、贴纸/相框及移动调整逻辑。


## V1.0.6
- 锁定最终「鲜活自然」方案：衣服/背景色彩更鲜明，肤色保持自然通透，降低偏红与过度白光。
- 保留 V1.0.4 已确认构图：人物尽可能大、整体适当下移、减少底部空白。
- Logo/课程成品搭配继续执行不遮挡真人主体的安全区规则。
- 保留原图级 4:3 高清导出、PNG 无损、自定义调色与常用配置记忆。
- 保留四课程最新默认搭配素材及去重结果。
